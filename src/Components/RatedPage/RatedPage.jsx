import { React, Component } from 'react';

import './RatedPage.css';

export default class SearchPage extends Component {
  render() {
    // const { data, loading, error } = this.state;

    // return <p className="rated-page">Rated</p>;

    return (
      <div className="view">
        <p>TOMATO</p>
      </div>
    );
  }
}
